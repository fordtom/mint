# TriCore-only fixture: `group` is a byte-only aggregate larger than one
# octet, so the EABI raises its alignment to 2 and pads its tail; `solo`
# stays a single-octet aggregate with 1-octet alignment at an odd offset.
[mint]
abi = "tricore-eabi-le"
[bytes.header]
start_address = 0
length = 0x100
[bytes.data]
x = { value = 1, type = "u8" }
solo.only = { value = 2, type = "u8" }
group.a = { value = 3, type = "u8" }
group.b = { value = 4, type = "u8" }
group.c = { value = 5, type = "u8" }
tail = { value = 6, type = "u8" }
